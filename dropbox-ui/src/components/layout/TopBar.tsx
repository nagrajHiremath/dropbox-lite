/**
 * UI-00: structural placeholder only. Breadcrumbs land in UI-02/UI-03, search
 * and the user menu land in later phases.
 */
export default function TopBar() {
  return <header className="flex h-14 shrink-0 items-center border-b px-4" />
}
